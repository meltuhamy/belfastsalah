<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
      Android 12 (API 31) replaced the launch-theme splash with the system
      SplashScreen API. Without this override Android ignores the configured
      background entirely and falls back to its defaults: the launcher icon,
      upscaled from 192px and blurry, on the default window background rather
      than the app's colour.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
